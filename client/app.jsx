import React from 'react';
import ReactDOM from 'react-dom';
import {AuthWrapper} from './AuthWrapper.jsx';

const domContainerNode = document.getElementById('container');
ReactDOM.render(<AuthWrapper />, domContainerNode);
